# Cutover: ProjectAria takes over :8200 (absorbed aria-shells). The old :8000 is
# retired; clients (UI/widget/CLI) and the Hermes MCP now target :8200.
[Service]
ExecStart=
ExecStart=%h/Development/ProjectAria/api/.venv/bin/uvicorn aria.main:app --host 0.0.0.0 --port 8200
